type Note = {
  _id: string,
  contents: string,
  createdAt: number,
  updatedAt: number
}

const note: Note = {
  _id: '1',
  contents: 'afdasdf',
  createdAt: 1,
  updatedAt: "32323",
}

var i = note.contents
console.log(i)
var math = Math

console.log(math.abs(1))
math.abs(x)

